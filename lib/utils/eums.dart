enum UserType { client, applicant, agent }

enum DirectOfferTypes { accepted, pending, declined }

enum DirectOffersOptions { all, accepted, declined }

enum JobPostsOptions { active, ended, archived }
